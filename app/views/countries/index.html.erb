<table border="1">
  <tr>
    <th>Flag</th>
    <th>Name</th>
    <th>Capital</th>
 </tr>
  <% @countries.each do |country| %>
      <td></td>
      <%# <%= image_tag(country['flag'], style: 'width: 200px') %> 
      <td><%= country['name'] %></td>
      <td><%= country['capital'] %></td>
    </tr>
  <% end %>
</table>